import 'package:flutter/material.dart';

class Helper {
  static void showLoginPopUp(BuildContext context) {
    showDialog(
        context: context,
        builder: (context) {
          return Container();
        });
  }
}
